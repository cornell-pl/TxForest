(library
  (public_name forest)
  (name forest)
  (flags (:standard -w -32))
  (libraries core re str)
  (preprocess (pps ppx_deriving.show ppx_deriving.make ppx_jane))
  (private_modules Filesystem EvalForest Utils)
  (modules Utils PosixFilesytem Filesystem EvalForest ForestIntf)
)
